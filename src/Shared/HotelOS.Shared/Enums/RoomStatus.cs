namespace HotelOS.Shared.Enums;

/// <summary>Occupancy state of a room (independent of how clean it is).</summary>
public enum RoomStatus
{
    Available = 0,
    Occupied = 1,
    OutOfService = 2
}
