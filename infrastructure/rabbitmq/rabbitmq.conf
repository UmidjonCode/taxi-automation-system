# Allow the default guest user to connect from outside localhost.
# Required because the services connect to the mapped host port, which the
# broker sees as a non-loopback connection.
loopback_users.guest = false
listeners.tcp.default = 5672
